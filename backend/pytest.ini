[pytest]
asyncio_mode = auto
markers =
    integration: Full-pipeline integration tests. Require live FastAPI + LLM + DB stack.
                 Excluded from regular CI via: pytest -m "not integration"
                 MANDATORY PRE-LAUNCH GATE: pytest -m integration tests/test_adversarial_owasp.py -v
